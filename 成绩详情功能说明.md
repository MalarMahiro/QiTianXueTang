# 成绩详情查看功能说明

## 功能概述

基于真实抓包文件（ua-api.7net.cc_2026_08_27_18_18_03.har），为七天学堂 Flutter 复刻项目添加了完善的**成绩详情查看功能**。

## 新增功能

### 1. 单科成绩详情页 (`exam_subject_detail_page.dart`)

**功能特性：**
- 显示所有科目的成绩列表
- 点击任意科目可查看详细数据
- 底部抽屉式面板展示单科详情
- 支持刷新数据

**展示内容：**
- 得分/满分
- 班级排名/年级排名
- 班级平均分/年级平均分
- 错题数（如有）

**UI 设计：**
- 科目列表采用卡片式设计，带首字母头像
- 底部详情面板采用抽屉式弹出效果
- 统计卡片使用主题色高亮显示

### 2. 考试详情页增强 (`exam_detail_page.dart`)

**改进内容：**
- 各科成绩卡片改为可点击的 ListTile
- 点击科目卡片跳转到单科详情页
- 优化了空数据状态展示
- 添加了科目首字母头像

### 3. 服务层扩展

**新增 API 调用：**

#### DioClient (`dio_client.dart`)
```dart
/// 获取单科列表 (请求侧GCM加密): POST Question/Subjects
Future<List<Map<String, dynamic>>?> getSubjects({
  required String examGuid,
  required String schoolGuid,
  required String grade,
})
```

#### ExamService (`exam_service.dart`)
```dart
/// 获取单科列表（请求侧GCM加密）：POST Question/Subjects
Future<List<Map<String, dynamic>>?> getSubjectList(String examId)
```

#### ExamProvider (`exam_provider.dart`)
```dart
/// 获取单科列表
Future<List<Map<String, dynamic>>?> loadSubjectList(String examId)
```

## 技术实现

### 加密方式
- **请求加密**：AES-GCM（带 IV）
- **响应加密**：AES-GCM（由拦截器自动解密）
- **请求头**：
  - `bn`: IV（Base64 编码）
  - `bp`: 加密后的请求参数

### 数据流程
```
用户点击科目卡片
    ↓
ExamDetailPage 导航到 ExamSubjectDetailPage
    ↓
ExamSubjectDetailPage 调用 loadSubjectList(examId)
    ↓
ExamProvider.loadSubjectList() → ExamService.getSubjectList()
    ↓
DioClient.getSubjects() 发送 POST 请求
    ↓
拦截器自动解密响应数据
    ↓
返回 List<Map<String, dynamic>>
    ↓
UI 展示科目列表和详情
```

### 关键代码

**1. 请求加密（DioClient）**
```dart
final iv = SecureCrypto.generateIv();
final ivBytes = base64.decode(iv);
final params = jsonEncode({
  'examGuid': examGuid,
  'schoolGuid': schoolGuid,
  'grade': grade,
});
final bp = SecureCrypto.aesGcmEncrypt(params, ivBytes);

final resp = await _dio.post(
  '${ApiConfig.baseScore}${ApiConfig.questionSubjects}',
  options: Options(headers: {
    'bn': iv,
    'bp': bp,
  }),
);
```

**2. 响应解密（拦截器）**
```dart
// 在 DioClient 的 InterceptorsWrapper.onResponse 中
if (data is Map && data['data'] is Map) {
  final inner = data['data'] as Map;
  if (inner['isEncrypt'] == true && inner['content'] != null) {
    final content = inner['content'].toString();
    String decrypted;
    if (inner['bn'] != null) {
      decrypted = SecureCrypto.aesGcmDecrypt(content, inner['bn'].toString());
    }
    inner['content'] = decrypted;
  }
}
```

## 文件清单

### 新增文件
- `lib/pages/exam/exam_subject_detail_page.dart` (292 行)

### 修改文件
- `lib/services/dio_client.dart` (+38 行)
- `lib/services/exam_service.dart` (+18 行)
- `lib/providers/exam_provider.dart` (+11 行)
- `lib/pages/exam/exam_detail_page.dart` (+50, -36 行)

## 使用方式

### 1. 进入考试详情页
```
首页 → 考试成绩 → 点击任意考试
```

### 2. 查看单科详情
```
考试详情页 → 点击任意科目卡片 → 进入单科详情页
```

### 3. 查看详情内容
```
单科详情页 → 底部面板显示：
- 得分: 85/100
- 班级排名: 5
- 年级排名: 12
- 班级平均: 78.5
- 年级平均: 72.3
- 错题数: 3
```

## 适配说明

### 支持的接口
- ✅ `/Question/Subjects` - 获取单科列表
- ✅ `/Question/ScoreReport` - 获取考试总分详情（已有）

### 字段兼容
```dart
// 支持多种字段名映射
subject['subjectName']  // 科目名称
subject['score']        // 得分
subject['fullScore']    // 满分
subject['classRank']    // 班级排名
subject['gradeRank']    // 年级排名
subject['classAvg']     // 班级平均
subject['gradeAvg']     // 年级平均
subject['wrongCount']   // 错题数
```

## 测试建议

### 1. 正常流程测试
- [ ] 登录后进入考试成绩列表
- [ ] 点击考试进入详情页
- [ ] 点击科目卡片进入单科详情页
- [ ] 验证数据正确显示

### 2. 异常情况测试
- [ ] 无科目数据时显示提示
- [ ] 网络错误时显示错误提示
- [ ] 加密解密失败时的处理

### 3. UI 测试
- [ ] 科目列表滚动流畅
- [ ] 底部面板弹出动画正常
- [ ] 刷新按钮功能正常
- [ ] 空数据状态展示正确

## 后续优化建议

1. **答题卡功能**
   - 调用 `/Question/AnswerCardUrl` 接口
   - 显示答题卡视图

2. **错题分析**
   - 调用 `/Question/Conclusion` 接口
   - 展示错题详情和知识点分析

3. **学校评级规则**
   - 调用 `/Question/SchoolRatingRules` 接口
   - 显示学校评级标准

4. **性能优化**
   - 添加科目列表缓存
   - 预加载下一页数据

## 依赖关系

```
ExamSubjectDetailPage
    ↓ 使用
ExamProvider.loadSubjectList()
    ↓ 调用
ExamService.getSubjectList()
    ↓ 调用
DioClient.getSubjects()
    ↓ HTTP 请求
Question/Subjects API
```

## 注意事项

1. **加密依赖**：功能依赖于 `SecureCrypto` 和 `QitianCrypto` 加密模块
2. **上下文依赖**：需要先调用 `ExamService.setContext()` 设置 schoolGuid 和 grade
3. **网络请求**：使用 HTTPS，需要有效的 Token 认证
4. **响应格式**：响应数据由拦截器自动解密，无需手动处理加密

## 版本信息

- **实现日期**：2025-08-27
- **抓包版本**：ua-api.7net.cc_2026_08_27_18_18_03.har
- **API 版本**：4.6.1
- **Flutter 版本**：兼容 Flutter 3.x

## 联系与反馈

如有问题或建议，请查看项目 README 或提交 Issue。
